// Desenvolva um programa que leia 15 números inteiros e positivos e mostre o maior deles.

#include <stdio.h>
#include <stdlib.h>

int main ()
{
    int cont, number, higher;
    higher = 0;
    
    for(cont = 1; cont <= 15; cont ++);

    {

        printf("enter a number: ");
        scanf("%d", &number);
        
        if(number > higher);

        {

            higher = number;
    
        {
    }
    printf("the largest of the numbers read = %d\n", higher); 

return 0;
        }
    }

}