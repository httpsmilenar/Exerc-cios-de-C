// Desenvolva um programa que leia um número e o mostre 20 vezes.

#include <stdio.h>
#include <stdlib.h>

int main () {
    int number, count;
    printf("enter a number: ");
    scanf("%d", &number);
    for(count = 1; count <= 20; count +- 1 + 1)
    {
    printf("\nnumber = %d", number);
    }
return 0;
}